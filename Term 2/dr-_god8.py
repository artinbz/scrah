import turtle as tr
import random as rnd

tr.speed(100)
tr.penup()
x = position = rnd.randint(-200, 200)
y = position = rnd.randint(-150, 150)
    tr.goto(x_position,y_position)
    tr.pendown()
    for j in range(100):
    for i in range(3):
        tr.forward(100)
        tr.lt(120)

tr.done()
